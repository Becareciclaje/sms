package com.gestor.sms.daos;

import java.util.List;

import org.hibernate.Criteria;
import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.Transaction;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Repository;

@Repository
public class SmsDAO extends GestorDao

{
	
	/*
	 * public void verificaLogin(Usuario usuario) throws Exception {
	 * if(!usuario.getUsuario().equals("pepe") ||
	 * !usuario.getClave().equals("1234")) throw new Exception();
	 * 
	 * 
	 * }
	 */

	
}
